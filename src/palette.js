const colorTheme = 'light';

export const palette = {
    bg: colorTheme === 'light' ? '#ffffff': '#000000',
    accent: colorTheme === 'light' ? '#99beff' : '#99beff',
    accent2: colorTheme === 'light' ? '#c9f2f2': '#c9f2f2',
};